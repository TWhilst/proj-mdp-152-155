// This jenkinsfile is used to automate the installation of kops on an EC2 instance using Ansible.
pipeline {
    agent any
    
    tools {
        maven 'maven'
    }
    
    environment {
        ANSIBLE_INVENTORY = '/home/ec2-user/jenkins/workspace/k8s-remove/hosts.ini'
        ANSIBLE_PLAYBOOK = '/home/ec2-user/jenkins/workspace/k8s-remove/04-remove-k8s-cluster.yml'
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'project-2',
                credentialsId: 'github',
                url: 'https://github.com/TWhilst/proj-mdp-152-155.git'
            }
        }
        
        stage('Remove k8s cluster') {
            steps {
                ansiblePlaybook(
                    playbook: ANSIBLE_PLAYBOOK,
                    inventory: ANSIBLE_INVENTORY,
                )
            }
        }
    }
}
