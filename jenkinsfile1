// This Jenkinsfile is used to automate the installation of kubectl on an EC2 instance using Ansible.
pipeline {
    agent any
    
    tools {
        maven 'maven'
    }
    
    environment {
        ANSIBLE_INVENTORY = '/home/ec2-user/jenkins/workspace/k8s/hosts.ini'
        ANSIBLE_PLAYBOOK = '/home/ec2-user/jenkins/workspace/k8s/02-install-kubectl.yml'
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'project-2',
                credentialsId: 'github',
                url: 'https://github.com/TWhilst/proj-mdp-152-155.git'
            }
        }
        
        stage('Install kubectl') {
            steps {
                ansiblePlaybook(
                    playbook: ANSIBLE_PLAYBOOK,
                    inventory: ANSIBLE_INVENTORY,
                )
            }
        }
    }
}
